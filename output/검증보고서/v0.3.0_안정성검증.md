---
version: "0.3.0"
build_at: 2026-04-27
build_sha: 96d9339
status: 131-10 완료 (D 영역만 131-20 대기)
task: 131-10
go_decision: GO
go_decided_at: 2026-04-27
---

# v0.3.0 배포 전 안정성 검증

> Phase 131의 7영역(A~G) 검증 보고. A·E는 본 태스크(131-10)에서 자동/grep으로 1차 채움. B·C·F·G는 사용자 사람 회귀 → 결과 기록. D는 131-20(publish 직후)에서 채움.

## 빌드 정보

| 항목 | 값 |
|------|-----|
| 패키지 | `@teamsparta/rich-editor` |
| 버전 | 0.3.0 |
| 빌드 SHA | `1877bd36e37360cdcadfdf7d9bf030d694fc583d` (브랜치 `dev/131-10`, base main) |
| 빌드 일시 | 2026-04-27 |
| 빌드 명령 | `pnpm -C packages/rich-editor build` |
| dist 합 크기 | 647,284 bytes (≈ 632 KB, 한도 5 MB) |
| dist 산출물 | `index.d.ts` 4.6 KB · `index.mjs` 394.6 KB · `index.cjs` 242.2 KB · `styles.css` 5.9 KB |

## 영역별 결과

| 영역 | 합격 기준 (요약) | 결과 | 세부 |
|------|---------------|------|------|
| A 자동 | dist 4종 존재 + 합 < 5MB + pack 일치 | **PASS** | `pnpm check:dist` 3/3 통과. dist 합 647 KB |
| B 수동 | 7개 확장 round-trip 보존 | **PASS** | 사용자 회귀: 5페이지 round-trip 보존 확인 (헤딩·리스트·링크·이미지·표 colspan/rowspan·코드블록·태스크리스트) |
| C 수동 | 7개 확장 기본 명령 OK | **PASS** | 사용자 회귀: 7확장 기본 명령 + `/tables` toolbar로 행/열 추가·삭제·셀 병합·분리까지 정상 |
| D 수동 | 별도 폴더 설치 + 타입 인식 | 대기 | 131-20에서 채움 (publish 직후) |
| E 수동 | dist/index.d.ts에 17종 export 라인 존재 | **PASS** | 값 2종 + 옵션 8종 + 핵심 7종 모두 export 라인 확인 (아래 상세) |
| F 수동 | 5종 부가 props 동작 | **PASS** | 사용자 회귀: `/basics` 토글로 placeholder·readOnly·autofocus·className·onEditorReady 5종 동작 확인 |
| G 수동 | playground 5페이지 console.error 0건 | **PASS** | 사용자 회귀: 5페이지 진입·기본 조작·저장/로드 시 콘솔 에러 0건 |

## A 영역 상세 (자동 PASS)

```
pnpm -C packages/rich-editor check:dist
> @teamsparta/rich-editor@0.3.0 check:dist
> node scripts/check-dist.mjs

PASS — 3/3 항목 통과
  ✓ 1) dist/ 4종 산출물 존재 + 크기 > 0
  ✓ 2) 4종 합 < 5MB (5,242,880 bytes)
  ✓ 3) pack --dry-run 결과가 files 필드와 일치 (best-effort)
dist size: 647,284 bytes
```

> pnpm 9는 `pack --dry-run`을 미지원하므로 스크립트 내부에서 `npm pack --dry-run`으로 폴백한다 (구현 시 확인).

## E 영역 상세 (수동 grep PASS)

`packages/rich-editor/dist/index.d.ts` export 라인:

**값 export 2종**
- `RichEditor` ✓ (line 124)
- `htmlSerializer` ✓ (line 124)

**옵션 인터페이스 8종** (line 124, type export)
- `CoreExtensionOptions` ✓
- `HeadingsExtensionOptions` ✓
- `ListsExtensionOptions` ✓
- `LinksExtensionOptions` ✓
- `CodeBlockExtensionOptions` ✓
- `TaskListExtensionOptions` ✓
- `ImageExtensionOptions` ✓
- `TablesExtensionOptions` ✓

**핵심 타입 7종**
- `RichEditorProps` ✓ (line 124)
- `ExtensionKey` ✓ (line 124)
- `ExtensionInput` ✓ (line 124)
- `ExtensionSpec` ✓ (line 124)
- `ExtensionOptionsMap` ✓ (line 124)
- `Serializer` ✓ (line 124)
- `Editor` ✓ (line 3 — `@tiptap/core` 재export)

총 17종 모두 존재. 0.x 비-브레이킹 약속 유지.

## B·C·F·G 영역 — 사용자 회귀 가이드

### 사전 준비

```bash
# 1) playground 가동
cd C:/dev/rich-editor-131-10
pnpm install     # 이미 완료됨
pnpm -C apps/playground dev
# → 브라우저에서 http://localhost:5173 접속
```

### B (round-trip 보존)

각 페이지에서 샘플 HTML 로드 → 편집 없이 "저장" → 페이지 새로고침 → "불러오기" → 편집기·`onChangeHtml 출력` 영역의 DOM 구조·텍스트·속성·중첩 일치 확인.

| 페이지 | 확장 | 특이 확인 |
|--------|------|---------|
| `/text` | headings · lists · links | 헤딩 레벨, 중첩 리스트, 링크 href·rel |
| `/code` | codeBlock · taskList | 코드블록 언어 클래스, 태스크 체크 상태 |
| `/images` | images | 더미 업로드 → src·alt·figure 구조 |
| `/tables` | tables | **colspan/rowspan 보존 필수** |

### C (기본 명령 동작)

| 페이지 | 명령 |
|--------|------|
| `/text` | H1↔H2↔H3 전환, 중첩 리스트 들이기/내보내기, 링크 단축키/다이얼로그 |
| `/code` | 언어 지정·줄바꿈, 태스크 체크박스 토글·중첩 |
| `/images` | 더미 훅으로 업로드·삽입·alt 입력 |
| `/tables` | "표 명령 toolbar" 버튼으로 — 표 삽입 → 셀 안에 커서 → 행/열 추가·삭제, 셀 병합(두 셀 드래그 선택 후), 셀 분리. `Tab`/`Shift+Tab`은 셀 이동 |

### F (부가 props)

`/basics` 페이지 상단 "F 영역 — 부가 props 검증 토글" 섹션:

| props | 확인 방법 |
|-------|----------|
| `placeholder` | input 입력값 변경 → 빈 에디터에 안내 문구 갱신 |
| `readOnly` | 체크 → 입력 차단·도구 비활성 |
| `autofocus` | 체크 후 `placeholder` 변경 → 재마운트되며 마운트 즉시 포커스 |
| `className` | DevTools Elements에서 `div.rte-editor.verify-classname-applied` 확인 |
| `onEditorReady` | 페이지 진입/재마운트 시 콘솔에 `[F-onEditorReady]` 로그 1회 |

### G (콘솔 에러 0건)

5개 페이지(`/basics·/text·/code·/images·/tables`) 진입 + 기본 조작 + 저장/불러오기/새로고침 시 DevTools Console:
- `console.error` 0건 (있으면 출처·내용 본 보고서 §FAIL 발생 이력에 기록)
- `console.warn` 발생 시 출처 평가 — 외부 라이브러리 의도된 경고면 허용 + 본 섹션에 명시

## FAIL 발생 이력

(FAIL 발견 시: 영역 → 원인 → 조치 → 재검증 결과 순서로 추가)

### 회귀 중 발견된 사전 결함 (131-10 내에서 해결)

1. **`/tables` 셀 병합/분리 검증 UI 부재**
   - 원인: `/tables` 페이지가 toolbar 없이 EditorPanel만 띄워 사용자가 mergeCells/splitCell을 트리거할 수 없었음
   - 조치: `/tables`에 표 명령 toolbar 8종(표 삽입·행/열 추가·삭제·셀 병합/분리·표 삭제) 영구 추가. `samples.ts`의 잘못된 단축키 안내(`Ctrl+Shift+방향키`) 정정
   - 커밋: `0cdadd7 feat: /tables 검증용 toolbar 추가`
   - 재검증: PASS

2. **`extensions={['tables']}` 등 'core' 미포함 호출 시 schema 누락 에러**
   - 원인: `mergePlaceholder`가 'core'를 자동 prepend하지 않아 Document·Paragraph·Text 노드 부재로 `Schema is missing its top node type ('doc')` 발생
   - 조치: `mergePlaceholder`가 입력에 'core' 없으면 맨 앞에 자동 주입하도록 수정. `linksExtensionFactory`의 HTMLAttributes 기본값 `undefined → {}`도 같이 정정
   - 커밋: `96d9339 fix: extensions에 'core' 자동 prepend + ...`
   - 재검증: 5페이지 모두 정상 마운트 + 입력·편집 동작 PASS

## 사용자 GO/NO-GO

**GO** — 2026-04-27

- A·B·C·E·F·G 6영역 모두 PASS 확인
- 131-20 착수 가능 (publish 실행 + D 영역 검증)
- D 영역만 publish 직후 131-20에서 채움
